{
  "mcpServers": {
    "memos": {
      "command": "uvx",
      "args": ["memos-mcp-enhanced"],
      "env": {
        "MEMOS_INSTANCE_URL": "http://localhost:5230",
        "MEMOS_API_TOKEN": "your_api_token_here"
      }
    }
  }
}
